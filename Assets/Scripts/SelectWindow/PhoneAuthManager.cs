//using Firebase.Auth;
using UnityEngine;
using UnityEngine.UI;
using TMPro;

public class PhoneAuthManager : MonoBehaviour
{
  /*  public TMP_InputField phoneNumberInput;
    public InputField verificationCodeInput;
    public TextMeshProUGUI statusText;

    private FirebaseAuth auth;
    private PhoneAuthProvider provider;

    private void Start()
    {
        auth = FirebaseAuth.DefaultInstance;
        provider = Firebase.Auth.PhoneAuthProvider.GetInstance(auth);
    }

    public void SendVerificationCode()
    {
        string phoneNumber = phoneNumberInput.text;

        provider.VerifyPhoneNumber(phoneNumber, 60, null, verificationCompleted: (credential) =>
        {
     
            statusText.text = "Verification completed.";
        }, verificationFailed: (error) =>
        {
            statusText.text = "Verification failed: " + error;
        }, codeSent: (id, token) =>
        {
            
            statusText.text = "Code sent successfully.";
        }, codeAutoRetrievalTimeOut: (id) =>
        {
            statusText.text = "Code auto-retrieval timed out.";
        });
    }
*/
   
}
